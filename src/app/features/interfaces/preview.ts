export interface PollPreview {
  name: string;
  version: string;
  cosmicLatteId: string;
  components: string[];
}
export interface StudentPreview {
  '#': number;
  name: string;
  email: string;
  cohort: string;
  save: boolean;
}
